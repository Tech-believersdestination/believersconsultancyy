// StateTabsPG component has been removed - using StateTabs for PG options only
// This file is kept as a placeholder to avoid import errors

import React from "react";

const StateTabsPG: React.FC<{ activeTab: string; onTabChange: (tab: string) => void }> = () => {
  return null; // Component disabled - use StateTabs instead
};

export default StateTabsPG;
